<?xml version="1.0" encoding="utf-8"?>
<odoo>


    <!-- Window Action -->
    <record id="action_hospital_female_patient" model="ir.actions.act_window">
        <field name="name">Female Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'default_gender' : 'female'} </field>
<!--        here your can add default value you want in your odoo form  and you can pass multiple value in form after add comma ( , )-->

        <field name="domain">[('gender' , '=' , 'female') ] </field>
<!--        , ('age' , '&lt;' , 5) NOTE : here you can add more filter condition -->

        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first female patient here now it only github testing
            </p>
        </field>
    </record>

      <menuitem id="menu_female_patient"
              name="female Patients"
              parent="menu_patient_master"
              action="action_hospital_female_patient"
              sequence="30"/>

</odoo>
